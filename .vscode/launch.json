{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "(cms) Listen for XDebug",
      "type": "php",
      "request": "launch",
      "port": 9003,
      "pathMappings": {
        "/workspaces/wp-standard": "${workspaceFolder}"
      }
    }
  ]
}
